import { Typography } from "@mui/material";
import React from "react";

const NoData = () => {
  return (
    <Typography marginTop={"24px"} paddingLeft={"24px"}>
      Không tìm thấy sản phẩm phù hợp
    </Typography>
  );
};

export default NoData;
